#!/usr/bin/env bash

# Aliases
alias vi='nvim'
alias vim='nvim'
alias vimdiff='nvim -d'
alias ls='ls -G'
alias rm='rm -i'
alias vlc='clear && /Applications/VLC.app/Contents/MacOS/VLC -I macosx --extraintf ncurses --video-on-top'
alias cddownloads='cd $HOME/Downloads/'

